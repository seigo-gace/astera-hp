import test from 'node:test';
import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
const read=path=>readFile(new URL(`../${path}`,import.meta.url),'utf8');
test('uploaded TOP visual sits on pure black without card or UI overlay',async()=>{const css=await read('site/assets/top-rebuild.css');const home=await read('site/templates/home.html');for(const marker of ['body[data-route="home"]{background:#000','background:#000','border:0','border-radius:0','box-shadow:none','.top-visual img'])assert.ok(css.includes(marker),`Missing TOP black-stage marker: ${marker}`);assert.match(home,/class="top-visual"/);assert.match(home,/astera-original-transparent\.svg/);assert.doesNotMatch(home,/<button[^>]*top-visual|<a[^>]*top-visual|<canvas|data-astera-hero|hero-copy|hero-concept/)});
test('side menu contains Main 8 and keeps one controller for open-close state',async()=>{const header=await read('site/templates/partials/header.html');const css=await read('site/assets/top-rebuild.css');const helper=await read('site/assets/navigation-canonical.js');const app=await read('site/assets/app.js');const base=await read('site/templates/base.html');const navStart=header.indexOf('<nav id="global-nav"');const navEnd=header.indexOf('</nav>',navStart);const productMenu=header.indexOf('id="product-menu"',navStart);assert.ok(navStart>=0&&navEnd>navStart,'Global navigation is missing');assert.ok(productMenu>navStart&&productMenu<navEnd,'Main 8 must be nested inside the side menu');assert.ok(header.indexOf('{{MAIN8_LINKS}}',productMenu)<navEnd,'Main 8 links are not inside the product menu');for(const marker of ['position:fixed','width:min(430px,100%)','height:100dvh','transform:translateX(105%)','.astera-side-menu.is-open'])assert.ok(css.includes(marker),`Missing side-menu marker: ${marker}`);assert.match(app,/navToggle\?\.addEventListener|navToggle\.addEventListener/);assert.match(app,/globalNav\?\.classList\.toggle\('is-open'/);assert.doesNotMatch(helper,/productMenu\.hidden\s*=\s*false|exposeMain8InMobileMenu/);assert.match(helper,/syncNavigationLabels/);assert.ok(base.includes('/assets/navigation-canonical.css'));assert.ok(base.includes('/assets/navigation-canonical.js'))});
