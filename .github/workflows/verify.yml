name: Verify direct static website

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: astera-hp-static-verify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify-static:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Verify static source after Main 10 removal
        shell: bash
        run: |
          set -euo pipefail
          for file in index.html style.css script.js styles/base.css styles/header.css styles/hero.css styles/menu.css styles/ai-bubble.css scripts/language.js scripts/menu.js scripts/ai-bubble.js scripts/app-entry.js scripts/hero-effect.js assets/brand/astera-logo-dark.svg assets/icons/ai-guide-robot.svg CNAME; do
            test -s "$file"
          done
          for removed in styles/main10.css styles/main10-icons.css scripts/main10-space.js assets/icons/main10/main10-icons.svg; do
            test ! -e "$removed"
          done
          ! grep -Fq 'main10' index.html
          ! grep -Fq 'main10' script.js
          ! grep -Fq 'data-main10-card' index.html
          node --check script.js
          test "$(cat CNAME)" = "asterav8.jp"
      - name: Verify local HTTP delivery
        shell: bash
        run: |
          set -euo pipefail
          python3 -m http.server 4173 --directory . >/tmp/astera-static.log 2>&1 &
          pid=$!
          trap 'kill "$pid" 2>/dev/null || true' EXIT
          for attempt in $(seq 1 20); do
            if curl --fail --silent http://127.0.0.1:4173/index.html >/tmp/index.html; then break; fi
            sleep 1
          done
          ! grep -Fq 'main10' /tmp/index.html
