# Approved visual asset boundary

Notion defines the approved visual pack and exact paths. Binary files are not accessible through the current integration, so semantic HTML visual components are used as accessible fallbacks and the production gate remains blocked.
