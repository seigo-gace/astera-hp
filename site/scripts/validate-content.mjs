import {readChunkedData} from './lib-data.mjs';const p=await readChunkedData(new URL('../data/',import.meta.url),'content-bundle.index.json');if(p.length!==26)throw Error('ROUTE_COUNT');const seen=new Set();for(const x of p){if(seen.has(x.route))throw Error('DUPLICATE_ROUTE');seen.add(x.route);if(x.sections.length<1)throw Error('EMPTY_CONTENT '+x.route);if(!x.title||!x.description||!x.h1)throw Error('METADATA_MISSING '+x.route);if(x.route!=='/'&&x.sections.length<3)throw Error('SECTION_UNDERSPECIFIED '+x.route)}const main=p.filter(x=>x.route.startsWith('/product/'));if(main.length!==8)throw Error('MAIN8_COUNT');console.log('Content PASS: 26 routes');
