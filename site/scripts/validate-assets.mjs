import {readFile} from 'node:fs/promises';const a=JSON.parse(await readFile(new URL('../data/asset-manifest.json',import.meta.url)));if(a.brand.length!==10||a.visual.length!==12)throw Error('ASSET_CONTRACT_COUNT');if(a.brand.some(x=>!x.status)||a.visual.some(x=>!x.status))throw Error('ASSET_STATUS_MISSING');console.log('Asset contract PASS; production binaries remain explicitly blocked');
